export enum SAVING_TYPE {
  "CARBON",
  "DIESEL",
}

export enum DEVICES {
  advenio = 1,
  approbo,
  ventito,
  ulterius,
  omnis,
  commemoro,
  comis,
  vitium,
  abeo,
  cubitum,
}

export enum STATUS {
  PENDING = "pending",
  SUCCESS = "success",
  FAILED = "failed",
}
